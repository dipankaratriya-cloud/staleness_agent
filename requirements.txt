# Core pipeline
python-dotenv
requests
playwright
openpyxl
pyarrow
groq

# GCP
google-cloud-bigquery
google-cloud-storage

# Existing deps
# dataclasses  ← built into Python 3.7+, no install needed
# datacommons  ← all PyPI versions yanked, not imported in pipeline
earthengine-api
geojson
geopy
google-cloud-logging
lxml
rasterio
requests_cache
retry
s2sphere
shapely
urllib3
pandas
